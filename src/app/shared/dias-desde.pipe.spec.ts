import { DiasDesdePipe } from './dias-desde.pipe';

describe('DiasDesdePipe', () => {
  it('create an instance', () => {
    const pipe = new DiasDesdePipe();
    expect(pipe).toBeTruthy();
  });
});
